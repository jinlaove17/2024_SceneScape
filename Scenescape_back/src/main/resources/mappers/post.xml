<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ssafy.enjoytrip.model.dao.BoardDAO">
  	<insert id="insert" useGeneratedKeys="true" keyProperty="no" parameterType="PostDTO">
  		insert into posts
  		values
  		(null, #{title}, #{content}, #{userId},
  		0, 0, 0, null, null, #{category}, #{thumbnailUrl})
  	</insert>
  	
  	<update id="update" parameterType="PostDTO">
  		update posts
  		set
  		title = #{title}, content = #{content},
  		thumbnail_url = #{thumbnail_Url}
  		where no = #{no}
  	</update>
  	
  	<delete id="delete" parameterType="int">
  		delete
  		from posts
  		where no = #{no}
  	</delete>
  	
  	
  </mapper>