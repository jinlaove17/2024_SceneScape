<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ssafy.enjoytrip.model.dao.AreaDAO">
    <select id="select" parameterType="int" resultType="com.ssafy.enjoytrip.model.dto.SubAreaDTO">
        SELECT s.sido_code AS areaCode, s.sido_name AS areaName, 
               g.gugun_code AS subAreaCode, g.gugun_name AS subAreaName
        FROM sidos s 
        JOIN guguns g ON s.sido_code = g.sido_code
        WHERE s.sido_code = #{areaCode}
    </select>
</mapper>
