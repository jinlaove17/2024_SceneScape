<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.ssafy.enjoytrip.model.dao.UserDAO">
 	<insert id="insert">
 		insert into user(id, pwd, nickname, email)
 		values(#{id}, #{pwd}, #{nickname}, #{email})
 	</insert>
 	
 	<update id="update">
 		update user
 		set    pwd = #{pwd}, nickname = #{nickname}, email = #{email}
 		where  id = #{id}
 	</update>
 	
 	<delete id="delete">
 		delete
 		from   user
 		where  id = #{id}
 	</delete>
 	
 	<select id="select">
 		select *
 		from   user
 		<where>
 			<if test="id != null and id != ''">
 				and id = #{id}
 			</if>
 			 <if test="nickname != null and nickname != ''">
 				and nickname = #{nickname}
 			</if>
 		</where>
 	</select>
 	
 	<select id="selectAll">
 		select *
 		from   user
 	</select>
 </mapper>